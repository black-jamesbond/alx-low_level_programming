#include<stdio.h>
/**
 * main- code for all
 *
 * Description: jkndcjndc
 *
 * Return: 0
 *
 */

int main(void)
{
	char alphabet;
	char ch;

	ch = '\n';
	for (alphabet = 'z'; alphabet >= 'a'; alphabet--)
	{
		putchar(alphabet);
	}
	putchar(ch);
	return (0);
}

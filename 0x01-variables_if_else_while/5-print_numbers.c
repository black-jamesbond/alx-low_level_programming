#include<stdio.h>
/**
 * main- to check a con
 *
 * Description: it is ti compare
 *
 * Return: 0
 *
 */

int main(void)
{
	int a;
	char ch;

	ch = '\n';

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	putchar(ch);
	return (0);
}

